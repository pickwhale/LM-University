Flyway migrations are intentionally disabled for this backend.

The application targets an existing production database schema from `university(1).sql`.
Do not add schema-changing migration scripts here unless the production database change
has already been approved and applied through the project's normal DBA process.
